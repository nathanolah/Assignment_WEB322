<div class="form-header"><h2>Login</h2></div>
<div class="form">
    {{#each errors}}
        <div class="errors">{{this}}</div>
    {{/each}}

    <div class="successMessage">{{successMessage}}</div>

    <form action="/user/login" method="POST">
        <ul class="form-style-1">
            <li>
                <label for="email">Email<span class="required">*</span></label>
                <input type="email" name="email" id="email" class="field-long">
            </li>
             <li>
                <label for="password">Password<span class="required">*</span></label>
                <input type="password" name="password" id="password" class="field-long" placeholder="At least 6 characters">
            </li>
            <li>
                <input type="submit" value="Login">
            </li>
        </ul>
    </form>
</div>