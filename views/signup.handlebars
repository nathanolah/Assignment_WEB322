<div class="form-header"><h2>Create an account</h2></div>
<div class="form">
    {{#each errors}}
        <div class="errors">{{this}}</div>
    {{/each}}

    <div class="successMessage">{{successMessage}}</div>
    
    <form action="/signup" method="POST">
        <ul class="form-style-1">
            <li>
                <label for="fullName">Full Name<span class="required">*</span></label>
                <input type="text" name="firstName" id="firstname" class="field-divided" placeholder="First">
                <input type="text" name="lastName" id="lastName" class="field-divided" placeholder="Last">   
            </li>
            <li>
                <label for="email">Email<span class="required">*</span></label>
                <input type="email" name="email" id="email" class="field-long">
            </li>
            <li>
                <label for="username">Username<span class="required">*</span></label>
                <input type="text" name="username" id="username" class="field-long">
            </li>
             <li>
                <label for="password">Password<span class="required">*</span></label>
                <input type="password" name="password" id="password" class="field-long">
            </li>
            <li>
                <input type="submit" value="Sign Up">
            </li>
        </ul>
    </form>
</div>
